--- a/folia-server/build.gradle.kts
+++ b/folia-server/build.gradle.kts
@@ -26,7 +_,7 @@
     minecraftVersion = providers.gradleProperty("mcVersion")
     gitFilePatches = false
     
-    val fork = forks.register("folia") {
+    val folia = forks.register("folia") {
         upstream.patchDir("paperServer") {
             upstreamPath = "paper-server"
             excludes = setOf("src/minecraft", "patches", "build.gradle.kts")
@@ -35,8 +_,23 @@
         }
     }
 
-    activeFork = fork
-
+    val traium = forks.register("traium") {
+        forks = folia
+        upstream.patchRepo("paperServer") {
+            upstreamRepo = folia.patchedRepo("paperServer")
+            patchesDir = rootDirectory.dir("traium-server/paper-patches")
+            outputDir = rootDirectory.dir("paper-server")
+        }
+
+        upstream.patchDir("foliaServer") {
+            upstreamPath = "folia-server"
+            excludes = setOf("src/minecraft", "paper-patches", "minecraft-patches", "build.gradle.kts", "build.gradle.kts.patch")
+            patchesDir = rootDirectory.dir("traium-server/folia-patches")
+            outputDir = rootDirectory.dir("folia-server")
+        }
+    }
+
+    activeFork = traium
 
     spigot {
         enabled = true
@@ -121,12 +_,12 @@
 
 sourceSets {
     main {
-        java { srcDir("../paper-server/src/main/java"); srcDir("../paper-server/src/generated/java") }
-        resources { srcDir("../paper-server/src/main/resources") }
+        java { srcDir("../folia-server/src/main/java"); srcDir("../paper-server/src/main/java"); srcDir("../paper-server/src/generated/java") }
+        resources { srcDir("../paper-server/src/main/resources"); srcDir("../folia-server/src/main/resources") }
     }
     test {
-        java { srcDir("../paper-server/src/test/java") }
-        resources { srcDir("../paper-server/src/test/resources") }
+        java { srcDir("../folia-server/src/test/java"); srcDir("../paper-server/src/test/java"); }
+        resources { srcDir("../folia-server/src/test/resources"); srcDir("../paper-server/src/test/resources"); }
     }
 }
 val log4jPlugins = sourceSets.create("log4jPlugins") {
@@ -153,7 +_,8 @@
 }
 
 dependencies {
-    implementation(project(":folia-api"))
+    implementation(project(":traium-api"))
+    implementation("com.electronwill.night-config:toml:3.6.6")
     implementation("ca.spottedleaf:concurrentutil:0.0.3")
     implementation("org.jline:jline-terminal-ffm:3.27.1") // use ffm on java 22+
     implementation("org.jline:jline-terminal-jni:3.27.1") // fall back to jni on java 21
@@ -206,10 +_,6 @@
     implementation("io.papermc:reflection-rewriter:$reflectionRewriterVersion")
     implementation("io.papermc:reflection-rewriter-runtime:$reflectionRewriterVersion")
     implementation("io.papermc:reflection-rewriter-proxy-generator:$reflectionRewriterVersion")
-
-    // Spark
-    implementation("me.lucko:spark-api:0.1-20240720.200737-2")
-    implementation("me.lucko:spark-paper:1.10.133-20250413.112336-1")
 }
 
 tasks.jar {
@@ -224,14 +_,14 @@
         val gitBranch = git.exec(providers, "rev-parse", "--abbrev-ref", "HEAD").get().trim()
         attributes(
             "Main-Class" to "org.bukkit.craftbukkit.Main",
-            "Implementation-Title" to "Folia",
+            "Implementation-Title" to "Traium",
             "Implementation-Version" to implementationVersion,
             "Implementation-Vendor" to date,
-            "Specification-Title" to "Folia",
+            "Specification-Title" to "Traium",
             "Specification-Version" to project.version,
-            "Specification-Vendor" to "Paper Team",
-            "Brand-Id" to "papermc:folia",
-            "Brand-Name" to "Folia",
+            "Specification-Vendor" to "TraiumMC Team",
+            "Brand-Id" to "traiummc:traium",
+            "Brand-Name" to "Traium",
             "Build-Number" to (build ?: ""),
             "Build-Time" to buildTime.toString(),
             "Git-Branch" to gitBranch,
@@ -395,7 +_,7 @@
 }
 
 fill {
-    project("folia")
+    project("traium")
     versionFamily(paperweight.minecraftVersion.map { it.split(".", "-").takeWhile { part -> part.toIntOrNull() != null }.take(2).joinToString(".") })
     version(paperweight.minecraftVersion)
 
